#ifndef PDB_
#define PDB_
#include <string>
#include <vector>
using namespace std;

#include "ahmet/bio/ptndb2.hh"
#pragma comment(lib,"libcore.lib")
#pragma comment(lib,"libbio.lib")


class pdb {
	/*
public:
	ptndb__ db;
	ptndb__pdbiter_ pdbiter;
	string file;
	void pdb():file(""){
	}
	//set comma-separated search paths to look for the file
	void setdatadir(stringdatadir){ db.setdatadir(datadir); }

	//set the global datapath
	static void SETDATADIR(string datadir){ ptndb__::DATADIR=datadir; }

	pdb(string file):file(file){
	}
	void loadfile(string file){
		db.loadpdb(file);
		ptn_ *p;
		ptndb__pdbiter_ pdbiter=db.getpdbiter();
	}
	ptn_* getfirstchain(){
	}
	ptn_* getnextchain(){
	}
	
	void print();
	*/
};

#endif
